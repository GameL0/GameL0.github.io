server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Redireciona todas as rotas para o index.html (necessário para SPAs)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
